import { AppLink } from '@/ui/common/link';

// `app/dashboard/page.tsx` is the UI for the `/dashboard` URL
export default function Login() {
	return (
		<>
			<AppLink href="/dashboad">mmm</AppLink>
			<h1>Hello, Dashboard Page!</h1>
		</>
	);
}
